# 推しスケ (oshi_suke) — プロジェクトルール

## これは何か
好きな作品（アニメ/漫画/ゲーム/VTuber等）を登録すると、
関連イベント・グッズ・予約開始/締切を集めて通知するFlutterアプリ。
現状はFirebase未導入のローカルMVP。イベントデータはGitHub Pages上のRemote JSON。

## 現在のフェーズ
「Androidアプリとして正常に動き、Playストアの内部テストに出せる状態」を目指す段階。
Firebase / GCP / Vertex AI / 推薦エンジンは**このフェーズでは導入しない**。

## 開発ルール（厳守）
1. 1回のタスクで1つの目的だけ扱う。関係ないリファクタリングを勝手に入れない。
2. 変更後は必ず `flutter analyze` → `flutter test` を実行し、結果を報告する。
3. 既存のテスト（40 passed）を壊さない。壊れたら原因を報告して止まる。
4. 依存パッケージを追加するときは、追加理由・代替案・サイズへの影響を先に説明し、承認を待つ。
5. 課金が発生しうるサービス（Firebase, GCP, 各種API）は勝手に導入しない。
6. 大きな変更の前に、変更するファイル一覧と方針を箇条書きで提示し、承認を待つ。
7. 私はプログラミング初心者。専門用語を使うときは一言で説明を添える。

## 環境
- Flutter SDK: D:\sdk\flutter
- リポジトリ: D:\dev\oshi_suke
- 実機: Android 15
- ビルドが通らないときは、コードより先に環境（JDK / Android SDK / local.properties）を疑う

## 非機能要件
- 匿名性重視。個人名・住所・個人メールがアプリやリポジトリに一切含まれないこと。
- 収益は広告のみ。アプリ内課金は導入しない。
